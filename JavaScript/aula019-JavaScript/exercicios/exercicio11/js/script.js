let base = Number(prompt("Insira a base de um triângulo"));
let altura = Number(prompt("Insira a altura de um triângulo"));

let area = (base * altura) / 2;

console.log("A área é:", area.toFixed(2));
