let base = Number(prompt("Insira a base de um retângulo"));
let altura = Number(prompt("Insira a altura de um retângulo"));

let perimetro = 2 * (base + altura);

console.log("O perímetro é:", perimetro.toFixed(2));
