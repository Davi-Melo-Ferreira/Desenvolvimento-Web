const somar = (a, b) => {
    console.log(`a + b = ${a+b}`);
};

const saudar = (nome) => {
    console.log(`Seja Bem-Vindo ${nome}`);
};

const dobrarNumero = (numero) =>{
    console.log(`O número ${numero} dobrado é ${numero*2}`);
};

soma = somar(1, 2);
saudacao = saudar("Davi");
dobro = dobrarNumero(5)